# iPhoneへ実際にインストールするための手順

このフォルダはPWA（ホーム画面Webアプリ）として完成しています。
必要なのは **HTTPSで公開すること** だけです。

## 方法A：Netlify等の静的サイトホスティング
1. このフォルダ一式、または配布ZIPを静的サイトホスティングへアップロードします。
2. 発行された `https://...` のURLをiPhoneのSafariで開きます。
3. 共有 → 「ホーム画面に追加」
4. 「Webアプリとして開く」をオン
5. 「追加」

## 方法B：GitHub Pages
このフォルダの中身をリポジトリ直下へ置き、Pagesを有効化してください。
`.nojekyll` も同梱しています。

## インストール後
- ホーム画面の「支援ナビ」アイコンから独立したWebアプリとして起動します。
- Service Workerにより主要ファイルをキャッシュします。
- 利用者情報・ABC記録・支援手順書は端末ブラウザの localStorage に保存されます。

## 実運用前の重要事項
利用者の個人情報や支援記録を業務で保存する場合、localStorageだけでの運用は推奨しません。
認証、暗号化、権限管理、バックアップ、端末紛失対策などを備えた保存方式へ移行してください。
